//Get globalData and a random index to check
var jsonData = pm.response.json();
var randomIndex = Math.floor(Math.random() * jsonData.length)

//Test to check animalGroupId is positive
pm.test("Check animalGroupId is positive", function () {
    pm.expect(jsonData[randomIndex].animalGroupId).to.greaterThan(0);
});

//Test to check mobId is positive
pm.test("Check mobId is positive", function () {
    pm.expect(jsonData[randomIndex].mobId).to.greaterThan(0);
});

//Test to check breedId is positive
pm.test("Check breedId is positive", function () {
    pm.expect(jsonData[randomIndex].breedId).to.greaterThan(0);
});

//Test to check animalTypeId is positive
pm.test("Check animalTypeId is positive", function () {
    pm.expect(jsonData[randomIndex].animalTypeId).to.greaterThan(0);
});

//Test to check animalGenderId is positive
pm.test("Check animalGenderId is positive", function () {
    pm.expect(jsonData[randomIndex].animalGenderId).to.greaterThan(0);
});

//Test to check revenueGroupId is positive
pm.test("Check revenueGroupId is positive", function () {
    pm.expect(jsonData[randomIndex].revenueGroupId).to.greaterThan(0);
});

//Test to check ownerId is positive
pm.test("Check ownerId is positive", function () {
    pm.expect(jsonData[randomIndex].ownerId).to.greaterThan(0);
});

//Test to check locationId is positive
pm.test("Check locationId is positive", function () {
    pm.expect(jsonData[randomIndex].locationId).to.greaterThan(0);
});

//Test to check yearBorn is valid year
pm.test("Check yearBorn is valid year", function () {
    pm.expect(jsonData[randomIndex].yearBorn).to.match(/^\d{4}$/);
});

//Test to check totalTally is positive
pm.test("Check totalTally is positive", function () {
    pm.expect(jsonData[randomIndex].totalTally).to.greaterThan(0);
});

//Test to check paddockId is positive
pm.test("Check paddockId is positive", function () {
    pm.expect(jsonData[randomIndex].paddockId).to.greaterThan(0);
});

//Test to check tally is positive
pm.test("Check tally is positive", function () {
    pm.expect(jsonData[randomIndex].tally).to.greaterThan(0);
});

